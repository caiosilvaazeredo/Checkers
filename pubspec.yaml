name: master_checkers
description: Master Checkers AI - Play checkers against Gemini AI
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase - Versões atualizadas
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  firebase_database: ^11.2.1
  
  # Google Sign In
  google_sign_in: ^6.2.2
  
  # Gemini AI
  google_generative_ai: ^0.4.7
  
  # State Management
  provider: ^6.1.2
  
  # Utils
  uuid: ^4.5.1
  intl: ^0.19.0
  shared_preferences: ^2.3.4

  # LAN Networking
  network_info_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
